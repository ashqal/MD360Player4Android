<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asha.md360player4android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoActivity" android:configChanges="screenSize|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="MDGLSurfaceViewDemoActivity" android:name=".MDGLSurfaceViewDemoActivity" android:configChanges="screenSize|orientation"/>
        <activity android:label="MD360RenderDemoActivity" android:name=".MD360RendererDemoActivity" android:configChanges="screenSize|orientation"/>
    </application>

</manifest>
